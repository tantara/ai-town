# pnpm config
# Hoist common types so jest / ts-jest can resolve them across packages.
public-hoist-pattern[]=*types*
public-hoist-pattern[]=jest*
public-hoist-pattern[]=ts-jest*
# Don't lint scripts on install (vendored .next/wrangler artifacts can fail).
strict-peer-dependencies=false
